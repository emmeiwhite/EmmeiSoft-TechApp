export default function TS() {
  return (
    <div className="h-[calc(100vh-12vh)]">
      <div className="max-w-5xl mx-auto px-5 py-6">
        <h1>🔹 What is Structural Typing / Duck Typing?</h1>

        <p> "If it walks like a duck and quacks like a duck, it's a duck."</p>

        <p>This means:</p>

        <blockquote>
          In TypeScript, an object is considered of a certain type if it has all the required
          structure (properties/methods), regardless of its explicit name or declared type.
        </blockquote>
      </div>
    </div>
  )
}
